# Infra

Local bootstrap values for Argo CD and Argo Rollouts used by the Makefile.

Files:
- `argocd/values.yaml`: Argo CD Helm overrides for local clusters.
- `rollouts/values.yaml`: Argo Rollouts Helm overrides for local clusters.
