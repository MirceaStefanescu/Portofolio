# Vault integration

This directory contains example policies for CI/CD pipelines and workloads.

- `policies/platform.hcl` grants read access to platform secrets.
- Use Vault Agent or CI plugins to inject short-lived tokens at runtime.
